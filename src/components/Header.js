import React from 'react';
import '../App.css';
import stellar from '../stellar.png'

const Header = () => {
  return (
    <header>
      <img src={stellar} alt='logo'></img>
    </header>
  );
};

export default Header;
